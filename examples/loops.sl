// StaticLang Loop Examples
// Demonstrates for loops, while loops, and variable scoping

func main() -> int {
    // While loop example
    var count int = 10;
    while (count > 0) {
        count = count - 1;
    }

    return count;
}